git add . significa la carpeta actual  es decir todos los archivos dentro de la carpeta git desde cero 
significa que guarda todos los archivos de la carpeta en la que estoy parado
git commit -m "lo que quiero que se guarde en la bitacora" es para poder guardar o agregar lo que queremos enviarle para que guarde
git log para ver lo que se ha guardado en mensajes
git commit -am "Para agregar el commit de un solo paso" 
gtt commit -am solo sirve para agregar archivo no agregar carpetas


Aprender a arepentirnos

git checkout index.html Para resetear los cambios en el index html
git checkout . elimina todo lo que hicimos en las carpetas anteriores

Quitar archivos del area de preparacion 
